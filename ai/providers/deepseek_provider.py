from .base_provider import BaseProvider


class DeepSeekProvider(BaseProvider):

    def generate(self, prompt):

        raise NotImplementedError(
            "DeepSeek Provider ainda não implementado."
        )