from .base_provider import BaseProvider


class ClaudeProvider(BaseProvider):

    def generate(self, prompt):

        raise NotImplementedError(
            "Claude Provider ainda não implementado."
        )